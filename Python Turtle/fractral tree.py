import turtle
turtle.turtles()


turtle.left(90)
turtle.speed(1)

#recursion
def draw():
    if(1<10):
        return
    else:
        foo.forward(1)
        foo.left(30)
        draw(3*1/4)
        foo.right(60)
        draw(3 * 1 / 4)
        foo.left(30)
        foo.backward(1)









